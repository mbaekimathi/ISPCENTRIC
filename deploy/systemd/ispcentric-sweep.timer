[Unit]
Description=Run the ISPCENTRIC expiry sweep every 3 minutes

[Timer]
OnBootSec=3min
OnUnitInactiveSec=3min
# Measured from the end of the last run, so slow runs cannot overlap.
AccuracySec=15s
Unit=ispcentric-sweep.service

[Install]
WantedBy=timers.target
